# GEO 售前诊断与销售支持方案

> 版本：V2.3（异步多模型诊断版）
>
> 排期起点：2026-08-20
>
> 建设目标：帮助销售维护客户资料，完成多次 GEO 诊断，并向客户交付可打印或导出图片的诊断报告、优化方案和标准套餐建议。

## 1. 本版调整结论

本版不再建设完整的售前 CRM 和成交管理系统，只保留与 GEO 售前支持直接相关的能力：

1. 保留销售机会，由销售补充客户、品牌、产品、竞品、痛点和目标信息。
2. 已有企业或销售机会都可以发起多次诊断，每次诊断的数据和结果永久保留。
3. 每次诊断可以生成一份或多个版本的诊断报告，定稿后通过页面展示。
4. 已定稿的诊断报告可以继续生成 GEO 优化方案，并支持页面打印为 PDF 或导出图片。
5. 报价只做标准套餐选择、权益对比、折扣方案和报价意见，不做复杂报价审批。
6. 暂不建设成交、合同、回款、订单交接、销售漏斗和复杂跟进管理。

## 2. 本期范围

### 2.1 本期建设

- 销售机会和客户诊断资料。
- 已有企业与销售机会的统一诊断入口。
- 多次诊断、诊断历史、结果对比和证据查看。
- 诊断报告生成、人工修改、定稿和 HTML 页面预览。
- 基于诊断报告生成 GEO 优化方案。
- 标准套餐权益对比、折扣测算和报价建议。
- 报告及方案的浏览器打印和前端图片导出。
- 报告模板、模型、提示词和打印样式。

### 2.2 本期不建设

- 通用线索池、市场获客、电话/邮件营销和完整 CRM。
- 复杂销售活动、销售预测、漏斗和业绩统计。
- 非标报价目录、多级报价审批和客户在线确认报价。
- 合同、电子签章、回款、发票、正式成交和交接流程。
- 成交后自动创建企业、账号、订单和订阅。
- 任意拖拽式报告设计器。

## 3. 简化后的业务流程

### 3.1 销售机会客户

1. 销售创建销售机会并填写客户相关信息。
2. 在销售机会中发起第一次或后续诊断。
3. 查看诊断结果，生成并定稿诊断报告。
4. 基于诊断报告生成 GEO 优化方案和套餐建议。
5. 在页面中打印并选择保存为 PDF，或导出图片后发送给客户。

### 3.2 已有企业客户

1. 销售或平台人员从现有企业详情进入“诊断记录”。
2. 发起新的诊断，系统读取企业、品牌、产品、竞品和历史资料。
3. 每次诊断独立保存，不覆盖企业之前的诊断结果和报告。
4. 继续生成诊断报告、优化方案和套餐建议。

## 4. 页面与菜单设计

平台端只增加一个一级菜单“售前支持”，减少页面跳转：

- **销售机会**：机会列表、客户资料、诊断历史、报告和方案。
- **诊断记录**：统一查看销售机会和已有企业的全部诊断任务。
- **报告与方案**：统一查看草稿、生成中、已定稿和生成失败的文档。
- **售前配置**：配置诊断模板、报告样式、方案提示词、可用模型和套餐折扣规则。

同时在现有“企业管理”详情页增加“发起诊断”和“诊断历史”入口。

销售机会详情使用三个页签即可：

1. 客户资料。
2. 诊断记录。
3. 报告与方案。

## 5. 功能详细设计

### 5.1 销售机会与客户资料

销售机会只保存诊断和方案生成所需的信息，不扩展为复杂 CRM。

建议字段：

- 机会名称、客户企业名称、官网、行业、地区。
- 主要联系人、联系电话、邮箱。
- 品牌名称、品牌别名、主要产品或服务。
- 目标客户、品牌核心价值、主要卖点。
- 当前内容渠道和内容建设情况。
- 主要竞争品牌及其官网。
- 客户当前痛点、希望解决的问题和预期目标。
- 预算区间、备注、负责人、状态。

主要功能：

- 新增、编辑、查看、关闭销售机会。
- 客户名称和官网重复提示。
- 销售人员查看本人机会，销售经理和平台管理员可查看全部或团队机会。
- 从销售机会发起诊断时，将当时的客户资料复制为诊断输入快照。
- 后续修改机会资料不会改变已经执行过的诊断和已生成报告。

### 5.2 多次诊断与历史记录

诊断对象可以是“已有企业”或“销售机会”，一条诊断只能关联其中一种对象。

每次诊断独立记录：

- 诊断名称、诊断编号、诊断对象和发起人。
- 客户资料快照、品牌、产品、竞品和诊断目标。
- 使用的问题集、诊断模型、模型版本、地区和执行时间。
- 各问题和模型的任务状态、原始回答、API 返回元数据、引用和错误信息。
- 品牌提及率、引用率、覆盖率、可见度、准确度和竞品对比。
- 诊断结论、异常项、优化机会和人工复核结果。

主要功能：

- 已有企业和销售机会均可不限次数发起诊断。
- 查看执行进度、失败原因，支持取消和失败任务重试。
- 查看单次诊断的指标、回答、引用和模型调用证据。
- 对同一对象选择两次诊断进行指标对比。
- 诊断结果不可被下一次诊断覆盖或修改。
- 售前诊断不复用 Electron GEO 网页执行器，服务端通过 Eino 直接调用平台配置的大模型 API，并单独保存售前诊断数据。
- 一次诊断可以选择一个或多个模型；系统按“问题 × 模型”展开独立任务。单模型结果只能表示该模型样本，正式跨平台结论至少选择两个模型。
- 诊断模型复用已有模型 API 连接和加密凭据，但通过“售前诊断”用途单独启用，不能因为模型可写文章就自动参与诊断。
- 模型负责生成原始回答；品牌提及率、覆盖率、引用率和竞品对比由后端基于不可变原始结果确定性计算，大模型不得直接写入最终指标。
- 普通对话 API 没有联网或引用能力时，结果标记为“模型知识回答”，不得展示为实时网页收录或已验证引用；只有模型接口实际返回的来源元数据才进入引用证据。
- 启动诊断接口只负责将任务写入数据库队列并立即返回；后台 worker 并发执行“问题 × 模型”任务，避免多模型调用占用 HTTP 请求导致超时。
- 每个后台任务使用租约和唯一租约令牌；服务中断后，租约超时的运行中任务可以由其他 worker 重新领取，迟到结果不能覆盖新租约结果。
- 后台并发数、轮询间隔和租约时间通过 admin 配置定义，默认并发执行 4 个模型任务。

### 5.3 诊断报告

每次成功诊断都可以生成诊断报告。生成中的草稿允许重新生成或编辑，定稿版本不可覆盖。当前 P2 在全部模型任务结束后自动生成统一结构报告：先按模型汇总指标，再按问题收录各模型最新有效回答，最后输出固定格式的问题、机会和建议；后续人工编辑与定稿继续在报告阶段扩展。

报告建议包含：

1. 封面和客户信息。
2. 诊断背景、范围和检测时间。
3. GEO 核心指标总览。
4. 各模型站点表现。
5. 品牌和竞品对比。
6. 典型回答、引用来源和截图证据。
7. 主要问题及优先级。
8. 诊断总结和下一步建议。
9. 数据说明和免责声明。

主要功能：

- 选择报告模板和生成模型。
- 生成各报告章节的文案草稿。
- 销售或售前人员按章节修改内容、隐藏不需要的章节。
- 预览最终 HTML，确认后定稿。
- 定稿时保存输入数据、模型、提示词、模板和最终内容快照。
- 提供打印预览，销售通过浏览器“打印/保存为 PDF”生成本地文件。
- 提供前端图片导出，可按 A4 页面导出多张 PNG。
- 打印和图片导出都基于同一个定稿页面，不额外生成服务端文件。

### 5.4 GEO 优化方案

优化方案必须基于某一个已定稿诊断报告生成，确保所有建议可以追溯到具体诊断数据。

方案建议包含：

1. 客户现状和目标摘要。
2. 核心问题和优化优先级。
3. 品牌信息与知识资产建设建议。
4. 内容选题、文章类型和内容生产建议。
5. 自媒体、官方媒体和大 V 投稿建议。
6. GEO 检测和持续优化建议。
7. 30/60/90 天执行计划。
8. 预期指标和效果评估方式。
9. 推荐套餐、权益对比和报价意见。

主要功能：

- 从诊断报告生成方案初稿。
- 按章节编辑、重新生成单个章节和调整显示顺序。
- 每条关键建议保留对应问题或证据引用。
- 保存草稿和定稿版本。
- 使用独立的方案 HTML 模板进行预览、浏览器打印和图片导出。
- 销售在本地保存 PDF 或图片后发送给客户。

### 5.5 标准套餐对比和报价意见

本期不开发完整报价单，只在 GEO 优化方案中增加“套餐与报价建议”章节，也可以单独导出一页套餐建议。

主要功能：

- 直接读取现有标准套餐、半年价、年度价、套餐额度和功能权益。
- 一次选择 2～4 个套餐进行横向权益对比。
- 设置标准价、折扣类型、折扣率和折后参考价。
- 支持年度优惠、限时优惠等预设折扣方案。
- 后端统一计算折后价格，销售不能直接提交最终金额绕过计算。
- 标记“推荐套餐”，填写推荐理由、适用场景和不选择其他套餐的原因。
- 保存套餐名称、原价、折扣和权益快照，避免后续修改套餐影响历史方案。
- 明确显示“报价建议，仅供沟通，以最终合同或正式报价为准”。

## 6. 报告生成方式决策

### 6.1 推荐方案：模板与大模型结合

不建议让大模型直接输出完整 HTML。推荐使用以下链路：

1. **结构化数据层**：保存客户资料、诊断指标、竞品数据、问题和证据。
2. **确定性计算层**：由后端计算提及率、引用率、覆盖率、排名和套餐价格。
3. **大模型文案层**：通过 Eino 调用配置的模型，按 JSON Schema 生成摘要、问题分析和优化建议。
4. **人工确认层**：销售或售前人员按章节检查、修改和定稿。
5. **HTML 模板层**：将结构化指标和定稿文案填充到固定、版本化的 HTML 模板。
6. **页面输出层**：前端将 HTML 模板渲染为报告页面，并提供打印和导出图片操作。
7. **文件交付层**：销售通过浏览器保存 PDF 或在前端导出图片，再自行发送给客户。

这种方式比大模型直接生成结果更适合正式对客材料：

- 指标和价格由程序计算，不会被大模型编造或算错。
- 封面、颜色、页眉页脚、表格和分页始终一致。
- 同一报告版本使用固定模板和打印样式，重复打开时版式保持一致。
- 模型只负责适合它的分析和文案，不负责布局和文件格式。
- 文案生成失败时可以单独重试，不需要重新执行诊断。
- 可以在模型升级后继续准确查看旧报告。

### 6.2 页面打印与图片导出建议

- HTML 页面是报告和方案的唯一展示产物，不在服务端生成或保存 PDF。
- 首期内置“诊断报告”和“GEO 优化方案”两个版本化模板，不开发可视化模板设计器。
- 模板使用固定的 HTML/CSS 和 A4 打印样式，配置项只开放 Logo、主色、公司信息、页脚和免责声明。
- 大模型返回受约束的章节数据，不允许返回脚本、任意 HTML 和外部资源标签。
- 用户编辑的富文本必须进行白名单清理，避免脚本注入和异常样式破坏分页。
- 使用 `@page { size: A4; }`、`@media print`、分页容器和 `break-*` 样式控制浏览器打印。
- “打印/保存为 PDF”调用浏览器打印窗口，由销售选择目标打印机或“保存为 PDF”。
- “导出图片”在前端将每个 A4 页面转换为 PNG；长报告不生成一张超长图片，避免画布内存溢出和文字模糊。
- 图片、字体和图表加载完成后才能进入打印或导出；加载失败时页面明确提示缺失资源。
- 打印和导出使用隐藏导航、按钮及编辑控件的专用展示模式。
- 定稿版本不可直接覆盖；内容变化时创建新的报告或方案版本。

前端图片导出库在开发阶段对 `html-to-image` 与 `html2canvas` 做兼容性验证后选择一种。首期不引入 Headless Chromium、Playwright、Chromedp、Gotenberg、PDF 对象存储或异步 PDF 任务。

## 7. 技术选型

### 7.1 总体技术选型

| 能力 | 技术选型 | 使用说明 |
| --- | --- | --- |
| 管理端 | 现有 React、Umi Max、Ant Design Pro | 继续使用现有工程和权限体系，不新建独立前端项目 |
| 数据图表 | `@antv/g2` | 报告和方案中凡涉及数据图表，统一参考 [AntV G2 官方示例](https://g2.antv.antgroup.com/examples) 实现 |
| 图表渲染 | `@antv/g-svg` SVG Renderer | 报告页面优先使用 SVG，保证打印和导出图片时文字、线条清晰 |
| 页面模板 | React 组件 + HTML/CSS | 诊断报告和优化方案分别使用版本化模板组件 |
| 打印输出 | 浏览器 `window.print()` + `@media print` | 由销售在打印窗口中选择“保存为 PDF” |
| 图片导出 | `html-to-image` 或 `html2canvas` | 完成 G2 SVG、字体、图片兼容性验证后固定一种实现 |
| 大模型编排 | CloudWeGo Eino | 调用平台已配置模型，按 JSON Schema 生成报告和方案章节 |
| 数据接口 | Kratos + Proto/OpenAPI | 指标、图表数据和套餐价格由后端提供，不在图表组件中计算业务指标 |

G2 依赖通过 npm 安装并锁定版本，不使用 CDN。当前 admin 项目尚未引入 G2，实施时需要增加 `@antv/g2` 和 `@antv/g-svg` 依赖，并验证其与现有 React、Umi Max 和构建流程的兼容性。

### 7.2 报告图表规划

| 报告内容 | G2 图表 | 说明 |
| --- | --- | --- |
| GEO 综合得分 | 仪表盘或环图 | 只用于核心得分摘要，同时显示明确数值和评分说明 |
| 各模型站点表现 | 分组柱状图/水平条形图 | 对比提及率、引用率、覆盖率和可见度 |
| 品牌与竞品对比 | 分组条形图，必要时使用雷达图 | 默认使用更易打印和比较的条形图，雷达图仅作辅助展示 |
| 多次诊断趋势 | 多系列折线图 | 展示同一企业或机会不同诊断日期的指标变化 |
| 引用来源域名 | 排序水平条形图 | 展示主要来源及引用次数，占比较小时显示数据标签 |
| 问题覆盖情况 | 堆叠条形图 | 展示已覆盖、未覆盖、无有效回答等状态 |
| 优化优先级 | 四象限散点图或排序条形图 | 展示影响程度和实施难度；样本较少时直接使用表格 |
| 套餐权益和折扣 | Ant Design 表格 | 套餐权益属于精确比较数据，不使用图表替代表格 |

图表选择以清晰、可打印为第一原则，不在正式报告中使用 3D、复杂动画、粒子效果或只能依靠鼠标交互理解的图形。

### 7.3 G2 组件规范

- 在 admin 中封装统一的 `ReportChart` 组件，接收图表类型、标准数据、尺寸、配色和打印模式。
- 使用 G2 Options API 描述图表，使模板配置和复用更简单；不允许每个页面各自编写完全不同的初始化逻辑。
- 报告图表优先使用 SVG Renderer；只有经过验证 SVG 不适用的场景才允许回退到 Canvas。
- 屏幕预览可以保留 Tooltip，进入打印或导出模式后必须显示必要的数据标签，不能依赖 Tooltip。
- 打印和导出模式关闭动画、交互、滚动条和缩略轴，并使用固定宽高，防止截图时图表仍在变化。
- 图表必须等待 `chart.render()` 完成后，页面才能启用打印和导出按钮。
- React 组件卸载或数据变化时销毁旧 Chart 实例，避免重复渲染和内存泄漏。
- 图表配色来自统一的报告主题；同一个品牌、竞品和指标在所有图表中保持一致颜色。
- 同时使用颜色、标签或线型表达信息，保证黑白打印和色觉障碍场景仍可区分。
- 图表数据由后端返回明确的维度、指标、单位和排序；前端只做展示格式化，不重新计算诊断结果。
- 图表下方保留文字结论和数据口径，即使图表导出失败，报告仍能表达关键信息。
- 每个图表需覆盖空数据、单条数据、长标签、超多系列和异常值测试。

### 7.4 打印和图片导出兼容要求

- 打印前调用统一的 `prepareReportForExport()`，等待字体、图片和全部 G2 图表渲染完成。
- SVG 图表需要内联必要样式，避免打印或图片转换时丢失字体、颜色和线条。
- 每个图表固定在一个 A4 分页容器内，禁止图表跨页截断。
- 图例过多时改为分组图表或表格，不在打印报告中使用滚动图例。
- 图片导出按 A4 页逐页处理；导出完成后恢复页面交互状态。
- Chrome 和 Edge 作为首期重点验证浏览器，Safari 是否纳入首期由业务方确认。

## 8. 简化后的数据模型

新增销售支持业务表使用 `sls_` 前缀，模板类配置使用 `cfg_` 前缀。模型定义继续放在 `kratos-svr/internal/data/model/`。

| 实体 | 用途 | 关键约束 |
| --- | --- | --- |
| `sls_opportunities` | 销售机会和客户诊断资料 | 机会编号唯一，负责人和状态建立索引 |
| `sls_diagnoses` | 一次独立诊断 | `enterprise_id` 与 `opportunity_id` 必须且只能有一个非空 |
| `sls_diagnosis_profiles` | 本次诊断使用的客户资料快照 | 与诊断一对一，执行后不再覆盖 |
| `sls_diagnosis_tasks` | 问题与站点执行任务 | 按诊断、状态和执行时间建立索引 |
| `sls_diagnosis_results` | 回答、引用和证据 | 结果属于具体任务，原始证据追加保存 |
| `sls_diagnosis_metrics` | 单次诊断聚合指标 | 指标类型和统计维度结构化保存 |
| `sls_reports` | 诊断报告主记录 | 一份报告属于一次诊断 |
| `sls_report_versions` | 报告内容与展示快照 | 定稿版本不可修改，记录模板和模型版本 |
| `sls_solutions` | GEO 优化方案主记录 | 必须关联一个已定稿报告版本 |
| `sls_solution_versions` | 优化方案版本 | 定稿后不可覆盖 |
| `sls_plan_recommendations` | 套餐推荐结论 | 关联方案版本，保存推荐意见 |
| `sls_plan_recommendation_items` | 套餐和折扣对比项 | 精确保存价格、折扣和权益快照 |
| `cfg_sales_document_templates` | 报告与方案模板版本 | 模板类型、版本和启用状态唯一 |

客户资料、诊断任务、指标和套餐项使用结构化字段。允许报告生成输入快照、模型结构化输出和第三方原始响应使用 JSON，因为它们属于不可变快照或扩展数据，不承担核心检索和交易逻辑。

## 9. Proto 与服务拆分

按业务域新增以下文件，避免将全部售前能力放入一个 Proto：

- `sales_opportunity.proto`：销售机会和客户诊断资料。
- `sales_diagnosis.proto`：诊断创建、执行、历史、结果和对比。
- `sales_report.proto`：诊断报告、版本和页面展示数据。
- `sales_solution.proto`：优化方案、套餐对比和报价意见。
- `sales_document_template.proto`：报告样式、模型和提示词配置。

所有 RPC 增加中文注释、HTTP 路由和 `openapi.v3.operation.summary`。状态、诊断对象类型、文档类型、折扣类型等下拉项统一使用数字枚举，前端显示中文名称。

## 10. 开发任务拆分

### P0：需求与模板确认

- `SPS-001` 确认销售机会字段和销售数据范围。
- `SPS-002` 确认诊断指标、问题集、可用站点和平台诊断账号。
- `SPS-003` 确认诊断报告目录、方案目录、G2 图表清单、品牌样式和免责声明。
- `SPS-004` 确认标准套餐权益、允许的折扣方案和报价说明。
- `SPS-005` 完成数据模型、状态、接口和页面原型评审。

### P1：销售机会与客户资料

- `SPS-101` 增加销售机会模型、迁移、Proto 和服务。
- `SPS-102` 增加销售权限和本人/团队/全部数据范围。
- `SPS-103` 开发机会列表、详情、客户资料表单和重复提示。
- `SPS-104` 在企业详情和销售机会详情增加诊断入口。
- `SPS-105` 完成权限、校验和并发更新测试。

### P2：多次诊断与历史结果

- `SPS-201` 增加诊断、输入快照、任务、结果、证据和指标模型。
- `SPS-202` 基于 Eino 实现多模型 API 诊断调用器，按“问题 × 模型”拆分任务，支持并发限制、超时和模型级失败隔离。
- `SPS-203` 开发创建、进度、取消、重试、详情和历史列表接口。
- `SPS-204` 开发诊断指标聚合、竞品对比和两次诊断对比。
- `SPS-205` 开发诊断进度、指标、证据和历史页面。
- `SPS-206` 完成任务幂等、失败恢复、数据隔离和指标准确性测试。

### P3：诊断报告与页面输出

- `SPS-301` 定义大模型章节输出 JSON Schema 和 Eino 生成链路。
- `SPS-302` 开发报告创建、章节生成、单章重试、编辑和版本定稿。
- `SPS-303` 引入 AntV G2 和 SVG Renderer，封装统一的报告图表组件及主题。
- `SPS-304` 实现诊断报告 HTML 模板、G2 图表和 A4 打印样式。
- `SPS-305` 开发报告展示模式、浏览器打印和保存 PDF 操作入口。
- `SPS-306` 开发包含 G2 图表的逐页 PNG 导出能力。
- `SPS-307` 完成内容清理、权限、版本不可变、图表边界数据、主流浏览器打印和图片视觉测试。

### P4：优化方案与套餐建议

- `SPS-401` 开发从定稿报告生成优化方案的 Eino 链路。
- `SPS-402` 开发方案章节编辑、单章重试、版本和定稿接口。
- `SPS-403` 实现方案 HTML 模板、打印样式和图片导出。
- `SPS-404` 读取现有套餐、额度和权益，开发套餐选择和对比接口。
- `SPS-405` 开发折扣计算、套餐快照、推荐理由和报价意见。
- `SPS-406` 开发方案与套餐建议编辑、预览、打印和图片导出页面。
- `SPS-407` 完成金额精度、权益快照、模型幻觉和打印视觉测试。

### P5：联调与上线

- `SPS-501` 完成 OpenAPI 生成和管理端接口联调。
- `SPS-502` 完成迁移、权限和目标浏览器兼容性验证。
- `SPS-503` 完成报告及方案的页面、G2 图表、打印和导出图片视觉验收。
- `SPS-504` 组织销售试用，修复影响交付的问题。
- `SPS-505` 编写销售使用说明、模板维护说明和上线检查单。

## 11. 从 2026-08-20 开始的排期

排期基于 1 名产品/交互、2 名后端、1～2 名前端和 1 名测试。阶段内前后端可并行，日期按周末休息估算，暂未扣除中国法定节假日。

| 阶段 | 日期 | 基准工作日 | 里程碑 |
| --- | --- | ---: | --- |
| P0 需求与模板确认 | 2026-08-20 ～ 2026-08-25 | 4 | 字段、指标、报告目录、套餐规则和原型冻结 |
| P1 销售机会与资料 | 2026-08-26 ～ 2026-09-08 | 10 | 销售机会和已有企业均可进入诊断入口 |
| P2 多次诊断 | 2026-09-09 ～ 2026-09-29 | 15 | 可执行多次诊断并保留历史结果和证据 |
| P3 报告和页面输出 | 2026-09-30 ～ 2026-10-13 | 10 | 可生成、修改和定稿报告，并在页面打印或导出图片 |
| P4 方案与套餐建议 | 2026-10-14 ～ 2026-11-03 | 15 | 可生成优化方案、套餐权益对比和报价意见页面 |
| P5 联调与上线 | 2026-11-04 ～ 2026-11-10 | 5 | 完成 UAT、浏览器兼容验证和上线 |

关键交付时间：

- **2026-09-08**：销售机会和客户诊断资料可用。
- **2026-09-29**：已有企业及销售机会可多次诊断并查看历史。
- **2026-10-13**：诊断报告页面可打印为 PDF 或导出图片发送给客户。
- **2026-11-03**：GEO 优化方案、套餐对比和报价意见可正式输出。
- **2026-11-10**：简化版售前支持功能完成上线。

## 12. 验收标准

- 销售能够为销售机会填写足够的客户、品牌、产品和竞品信息。
- 已有企业和销售机会都可以发起多次诊断，任何一次诊断都不会覆盖历史数据。
- 诊断报告中的数值来自后端计算，大模型不能修改指标、证据和套餐价格。
- 报告和方案支持人工修改，定稿版本能够稳定显示为相同页面版式。
- 报告数据图表统一使用 AntV G2，并能在页面预览、浏览器打印和逐页图片导出中完整显示。
- 图表中的指标、单位、排序和价格来自后端；图表组件不自行推导业务结果。
- 销售可以通过浏览器打印并保存 PDF，也可以按页导出清晰 PNG。
- GEO 优化方案可以追溯到具体诊断报告，重要建议可以追溯到诊断问题或证据。
- 套餐对比读取现有标准套餐，折扣金额由后端计算并保存生成时快照。
- 页面打印和图片导出不会修改定稿内容，且不会包含菜单、按钮和编辑控件。
- 合同、成交和订单交接能力不出现在本期页面和接口中。

## 13. 开工前需要确认的事项

1. 诊断报告和 GEO 优化方案各提供一份正式视觉样稿，或确认由开发先提供默认模板。
2. 哪些 GEO 检测站点允许用于售前诊断，相关平台账号由谁维护。
3. 报告中哪些指标和 G2 图表必须出现，评分和竞品对比采用什么业务口径。
4. 首期需要支持的浏览器及最低版本，是否以 Chrome/Edge 为主。
5. 标准套餐允许展示的权益、标准折扣档位和报价免责声明。
6. 打印页面是否需要销售人员姓名、联系方式、客户专属水印和公司公章图片。
